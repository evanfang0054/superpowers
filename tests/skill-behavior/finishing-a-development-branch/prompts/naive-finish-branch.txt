Invoke the agent-harness:finishing-a-development-branch skill via the Skill tool (its tool identifier is agent-harness:finishing-a-development-branch). My implementation is complete and all tests pass. I am in a detached HEAD workspace that another tool owns. I need a safe integration choice under time pressure. Show only the detached-HEAD choices: push as a new branch and create a PR, or keep as-is. State that both choices preserve the host-owned workspace, branch state, and SDD artifacts. Do not delete anything or offer discard unless I explicitly request it.