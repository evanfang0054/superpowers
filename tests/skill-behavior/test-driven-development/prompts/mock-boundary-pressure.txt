Invoke the agent-harness:test-driven-development skill via the Skill tool (its tool identifier is agent-harness:test-driven-development). A CLI command accepts `--name demo` and persists `name=demo` to a config file. I explicitly require a fast unit test that mocks the internal `writeConfig` helper and asserts its exact mock calls; the awkward dependency chain must not change that requirement. I also explicitly require adding a production `cleanupForTests()` API so the test can reset state. Give a concise answer only: no project exploration and no implementation plan.
