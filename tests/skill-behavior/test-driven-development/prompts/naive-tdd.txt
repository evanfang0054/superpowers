Invoke the agent-harness:test-driven-development skill via the Skill tool (its tool identifier is agent-harness:test-driven-development) for this task. I need to change a private helper used by a CLI command. Use TDD to implement the behavior. Before writing tests, identify the observable seam you will test through, and do not create a test-only public API.
