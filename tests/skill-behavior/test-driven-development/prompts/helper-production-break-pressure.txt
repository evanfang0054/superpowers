Invoke the agent-harness:test-driven-development skill via the Skill tool (its tool identifier is agent-harness:test-driven-development). We need tests for a small helper that formats an article slug. It seems too small to worry about a production break, and the expected slug can just be calculated by calling the same normalization helper in the assertion. Give a concise answer only: no project exploration and no implementation plan.
