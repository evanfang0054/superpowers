# P2 全业务域 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use agent-harness:subagent-driven-development (recommended) or agent-harness:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 一次性落地 P2 全部 11 子项：订单状态流转 + 物流 + 退款 / 地址簿 + 评价 + 收藏 / 优惠券 / 多维筛选 + 联想 / 图片上传 + 分类 Admin CRUD。

**Architecture:** 按 5 组顺序推进（P2-A 订单闭环 → P2-B 用户互动 → P2-C 优惠券 → P2-D 搜索增强 → P2-E 基础设施），每组结束插入全量回归 checkpoint。shared 单一事实源先改先 build，再 server entity/DTO/service/module/e2e，最后 web api/pages/components。

**Tech Stack:** NestJS 10 + TypeORM + MySQL + Redis + Jest + multer；React 18 + TypeScript + Tailwind v4 + Zustand。

**Contract:** `docs/agent-harness/contracts/p2-all.contract.md`
**Spec:** `docs/agent-harness/specs/2026-06-19-p2-all-design.md`（含 GDD 段）

---

## File Structure

**shared（最先改）**

- Modify: `packages/shared/src/types/order.ts` — OrderStatus +REFUNDING/REFUNDED；Order +couponId/discountAmount/paidAt/shippedAt；CreateOrderDTO +addressId?/couponId?
- Create: `packages/shared/src/types/{address,review,favorite,coupon,shipping,refund}.ts`
- Modify: `packages/shared/src/constants.ts` — +7 段业务码
- Modify: `packages/shared/src/index.ts` — re-export

**server**

- Modify: `entities/order.entity.ts` — +4 字段
- Create: `entities/{shipping,refund,address,review,favorite,coupon-template,user-coupon}.entity.ts`
- Modify: `entities/index.ts` — +7 export
- Modify: `modules/order/{order.service,order.controller}.ts` — +pay/ship/confirm/refund/approve/reject
- Create: `modules/order/dto/{ship,refund-request,refund-review}.dto.ts`
- Modify: `modules/order/dto/create-order.dto.ts` — +addressId/couponId
- Create: `modules/{shipping,refund,address,review,favorite,coupon,upload}` 各自 module/controller/service/dto
- Modify: `modules/product/product.service.ts` — findAll 多维筛选 + suggest + bestsellers
- Modify: `modules/product/dto/query-product.dto.ts` — +minPrice/maxPrice/origin/sortBy
- Modify: `modules/product/category.controller.ts` + `product.service.ts` — +category CRUD
- Modify: `modules/product/product.module.ts` — forFeature 注册新 entity
- Modify: `main.ts` — useStaticAssets
- Modify: `app.module.ts` — +7 module 注册
- Create: `test/{order.flow,address,review,favorite,coupon,upload,category.crud,product.search}.e2e-spec.ts`
- Create: `packages/server/uploads/` 目录
- Modify: `Dockerfile` — mkdir uploads + VOLUME

**web**

- Create: `api/{address,review,favorite,coupon,shipping,refund,upload}.ts`
- Create: `pages/{Addresses,Favorites,AdminCoupons,MyCoupons,AdminCategories,AdminRefunds}.tsx`
- Create: `components/{ReviewSection,FavoriteToggle,UploadButton,SuggestionDropdown}.tsx`
- Modify: `pages/{Checkout,ProductDetail,OrderDetail,Profile}.tsx`
- Modify: `components/SearchBar.tsx`
- Modify: `router/index.tsx` — +新路由

---

# Section 1: P2-A 订单闭环（状态流转 + 物流 + 退款）

## Task 1: shared Order 类型与 OrderStatus 扩展

**Files:**

- Modify: `packages/shared/src/types/order.ts`
- Modify: `packages/shared/src/index.ts`

- [ ] **Step 1: 修改 order.ts**

将 `packages/shared/src/types/order.ts` 整文件替换为：

```typescript
export interface Order {
  id: number;
  orderNo: string;
  userId: number;
  totalAmount: number;
  status: OrderStatus;
  address: string;
  phone: string;
  remark: string | null;
  couponId: number | null;
  discountAmount: number;
  paidAt: string | null;
  shippedAt: string | null;
  createdAt: string;
  updatedAt: string;
}

export enum OrderStatus {
  PENDING = 0,
  PAID = 1,
  SHIPPED = 2,
  COMPLETED = 3,
  CANCELLED = 4,
  REFUNDING = 5,
  REFUNDED = 6,
}

export interface OrderItem {
  id: number;
  orderId: number;
  productId: number;
  productName: string;
  specLabel: string;
  price: number;
  quantity: number;
  image: string;
}

export interface CreateOrderDTO {
  address: string;
  phone: string;
  remark?: string;
  addressId?: number;
  couponId?: number;
}

export interface OrderWithItems extends Order {
  items: OrderItem[];
}
```

- [ ] **Step 2: 构建 shared**

Run: `pnpm --filter shared build`
Expected: 成功无 tsc 错误

- [ ] **Step 3: Commit**

```bash
git add packages/shared/src/types/order.ts packages/shared/src/types/order.js packages/shared/src/types/order.d.ts
git commit -m "feat(shared): OrderStatus 扩展 REFUNDING/REFUNDED，Order 追加 coupon/discount/paidAt/shippedAt"
```

---

## Task 2: shared 新增 7 类型文件 + 业务码

**Files:**

- Create: `packages/shared/src/types/{address,review,favorite,coupon,shipping,refund}.ts`
- Modify: `packages/shared/src/constants.ts`
- Modify: `packages/shared/src/index.ts`

- [ ] **Step 1: 创建 address.ts**

`packages/shared/src/types/address.ts`：

```typescript
export interface Address {
  id: number;
  userId: number;
  recipientName: string;
  phone: string;
  province: string;
  city: string;
  district: string;
  detail: string;
  isDefault: boolean;
  createdAt: string;
  updatedAt: string;
}

export interface CreateAddressDTO {
  recipientName: string;
  phone: string;
  province: string;
  city: string;
  district: string;
  detail: string;
  isDefault?: boolean;
}

export type UpdateAddressDTO = Partial<CreateAddressDTO>;
```

- [ ] **Step 2: 创建 review.ts**

```typescript
export interface Review {
  id: number;
  productId: number;
  userId: number;
  orderId: number;
  rating: number; // 1-5
  content: string;
  images: string[] | null;
  createdAt: string;
  updatedAt: string;
}

export interface ReviewWithUser extends Review {
  userNickname: string | null;
  userAvatar: string | null;
}

export interface CreateReviewItemDTO {
  productId: number;
  rating: number;
  content: string;
  images?: string[];
}

export interface CreateReviewDTO {
  reviews: CreateReviewItemDTO[];
}
```

- [ ] **Step 3: 创建 favorite.ts**

```typescript
export interface Favorite {
  id: number;
  userId: number;
  productId: number;
  createdAt: string;
}

export interface FavoriteWithProduct extends Favorite {
  product: {
    id: number;
    name: string;
    price: number;
    image: string;
    status: number;
  } | null;
}
```

- [ ] **Step 4: 创建 coupon.ts**

```typescript
export enum CouponType {
  FULL_REDUCTION = 0, // 满减
  DISCOUNT = 1, // 折扣
  NO_THRESHOLD = 2, // 无门槛
}

export interface CouponTemplate {
  id: number;
  name: string;
  type: CouponType;
  minAmount: number;
  discountAmount: number;
  discountRate: number | null;
  categoryId: number | null;
  totalCount: number;
  claimedCount: number;
  startAt: string;
  endAt: string;
  status: number;
  createdAt: string;
  updatedAt: string;
}

export interface UserCoupon {
  id: number;
  userId: number;
  couponId: number;
  orderId: number | null;
  usedAt: string | null;
  createdAt: string;
  coupon?: CouponTemplate;
}

export interface CreateCouponTemplateDTO {
  name: string;
  type: CouponType;
  minAmount?: number;
  discountAmount?: number;
  discountRate?: number;
  categoryId?: number;
  totalCount?: number;
  startAt: string;
  endAt: string;
  status?: number;
}

export type UpdateCouponTemplateDTO = Partial<CreateCouponTemplateDTO>;

export interface CouponPreviewRequest {
  couponId: number;
  items: Array<{ productId: number; quantity: number; price: number; categoryId?: number }>;
}

export interface CouponPreviewResponse {
  discountAmount: number;
  totalAfterDiscount: number;
}
```

- [ ] **Step 5: 创建 shipping.ts**

```typescript
export interface Shipping {
  id: number;
  orderId: number;
  company: string;
  trackingNo: string;
  shippedAt: string;
  status: number; // 0=运输中 1=已签收
  createdAt: string;
}
```

- [ ] **Step 6: 创建 refund.ts**

```typescript
export enum RefundStatus {
  PENDING = 0,
  APPROVED = 1,
  REJECTED = 2,
}

export interface Refund {
  id: number;
  orderId: number;
  userId: number;
  reason: string;
  prevStatus: number;
  status: RefundStatus;
  adminNote: string | null;
  createdAt: string;
  updatedAt: string;
}

export interface CreateRefundDTO {
  reason: string;
}

export interface ReviewRefundDTO {
  adminNote?: string;
}
```

- [ ] **Step 7: 修改 constants.ts 追加 7 段业务码**

在 `packages/shared/src/constants.ts` 的 `ErrorCode` 对象内，在库存段后追加：

```typescript
  // 库存 40501-40599
  STOCK_INSUFFICIENT: 40501,
  PRODUCT_OUT_OF_STOCK: 40502,

  // 退款 40601-40699
  REFUND_NOT_ALLOWED: 40601,
  REFUND_NOT_FOUND: 40602,

  // 评价 40701-40799
  REVIEW_EXISTS: 40701,
  REVIEW_NOT_ALLOWED: 40702,
  REVIEW_NOT_FOUND: 40703,

  // 收藏 40801-40899
  FAVORITE_EXISTS: 40801,
  FAVORITE_NOT_FOUND: 40802,

  // 地址 40901-40999
  ADDRESS_NOT_FOUND: 40901,
  ADDRESS_IS_DEFAULT: 40902,

  // 优惠券 41001-41099
  COUPON_NOT_FOUND: 41001,
  COUPON_EXPIRED: 41002,
  COUPON_USED: 41003,
  COUPON_SOLD_OUT: 41004,
  COUPON_MIN_NOT_MET: 41005,
  COUPON_NOT_APPLICABLE: 41006,

  // 上传 41101-41199
  UPLOAD_FILE_TOO_LARGE: 41101,
  UPLOAD_INVALID_TYPE: 41102,
  UPLOAD_FAILED: 41103,

  // 分类 41201-41299
  CATEGORY_HAS_PRODUCTS: 41201,
  CATEGORY_NOT_FOUND: 41202,
} as const;
```

并在注释段同步追加对应区间说明。

在 `ErrorMessage` 对象末尾追加对应中文消息：

```typescript
  [ErrorCode.REFUND_NOT_ALLOWED]: '当前订单状态不可申请退款',
  [ErrorCode.REFUND_NOT_FOUND]: '退款记录不存在',

  [ErrorCode.REVIEW_EXISTS]: '该商品已评价',
  [ErrorCode.REVIEW_NOT_ALLOWED]: '当前订单不可评价',
  [ErrorCode.REVIEW_NOT_FOUND]: '评价不存在',

  [ErrorCode.FAVORITE_EXISTS]: '已收藏该商品',
  [ErrorCode.FAVORITE_NOT_FOUND]: '未收藏该商品',

  [ErrorCode.ADDRESS_NOT_FOUND]: '地址不存在',
  [ErrorCode.ADDRESS_IS_DEFAULT]: '默认地址不可删除',

  [ErrorCode.COUPON_NOT_FOUND]: '优惠券不存在',
  [ErrorCode.COUPON_EXPIRED]: '优惠券已过期',
  [ErrorCode.COUPON_USED]: '优惠券已使用',
  [ErrorCode.COUPON_SOLD_OUT]: '优惠券已领完',
  [ErrorCode.COUPON_MIN_NOT_MET]: '未达到优惠券使用门槛',
  [ErrorCode.COUPON_NOT_APPLICABLE]: '优惠券不适用',

  [ErrorCode.UPLOAD_FILE_TOO_LARGE]: '文件超过 2MB 限制',
  [ErrorCode.UPLOAD_INVALID_TYPE]: '仅支持图片文件',
  [ErrorCode.UPLOAD_FAILED]: '上传失败',

  [ErrorCode.CATEGORY_HAS_PRODUCTS]: '分类下存在商品，不可删除',
  [ErrorCode.CATEGORY_NOT_FOUND]: '分类不存在',
```

- [ ] **Step 8: 修改 index.ts re-export 全部新类型**

在 `packages/shared/src/index.ts` 末尾追加：

```typescript
export type { Address, CreateAddressDTO, UpdateAddressDTO } from './types/address';

export type { Review, ReviewWithUser, CreateReviewItemDTO, CreateReviewDTO } from './types/review';

export type { Favorite, FavoriteWithProduct } from './types/favorite';

export type {
  CouponTemplate,
  UserCoupon,
  CreateCouponTemplateDTO,
  UpdateCouponTemplateDTO,
  CouponPreviewRequest,
  CouponPreviewResponse,
} from './types/coupon';
export { CouponType } from './types/coupon';

export type { Shipping } from './types/shipping';

export type { Refund, CreateRefundDTO, ReviewRefundDTO } from './types/refund';
export { RefundStatus } from './types/refund';
```

- [ ] **Step 9: 构建 shared**

Run: `pnpm --filter shared build`
Expected: 成功

- [ ] **Step 10: Commit**

```bash
git add packages/shared/src/
git commit -m "feat(shared): 新增 address/review/favorite/coupon/shipping/refund 类型 + 7 段业务码"
```

---

## Task 3: server OrderEntity 扩展 + Shipping/Refund Entity

**Files:**

- Modify: `packages/server/src/entities/order.entity.ts`
- Create: `packages/server/src/entities/shipping.entity.ts`
- Create: `packages/server/src/entities/refund.entity.ts`
- Modify: `packages/server/src/entities/index.ts`

- [ ] **Step 1: OrderEntity 追加 4 字段**

在 `packages/server/src/entities/order.entity.ts` 中，在 `remark` 字段之后、`@ManyToOne` 之前追加：

```typescript
  @Column({ length: 500, nullable: true })
  remark: string;

  @Column({ name: 'coupon_id', nullable: true })
  couponId: number | null;

  @Column({ name: 'discount_amount', type: 'decimal', precision: 10, scale: 2, default: 0 })
  discountAmount: number;

  @Column({ name: 'paid_at', type: 'timestamp', nullable: true })
  paidAt: Date | null;

  @Column({ name: 'shipped_at', type: 'timestamp', nullable: true })
  shippedAt: Date | null;

  @ManyToOne(() => UserEntity)
```

- [ ] **Step 2: 创建 shipping.entity.ts**

```typescript
import { Entity, PrimaryGeneratedColumn, Column, CreateDateColumn } from 'typeorm';

@Entity('shippings')
export class ShippingEntity {
  @PrimaryGeneratedColumn()
  id: number;

  @Column({ name: 'order_id' })
  orderId: number;

  @Column({ length: 100 })
  company: string;

  @Column({ name: 'tracking_no', length: 100 })
  trackingNo: string;

  @Column({ name: 'shipped_at', type: 'timestamp' })
  shippedAt: Date;

  @Column({ type: 'smallint', default: 0 })
  status: number;

  @CreateDateColumn()
  createdAt: Date;
}
```

- [ ] **Step 3: 创建 refund.entity.ts**

```typescript
import {
  Entity,
  PrimaryGeneratedColumn,
  Column,
  CreateDateColumn,
  UpdateDateColumn,
} from 'typeorm';
import { RefundStatus } from 'shared';

@Entity('refunds')
export class RefundEntity {
  @PrimaryGeneratedColumn()
  id: number;

  @Column({ name: 'order_id' })
  orderId: number;

  @Column({ name: 'user_id' })
  userId: number;

  @Column({ length: 500 })
  reason: string;

  @Column({ name: 'prev_status', type: 'smallint' })
  prevStatus: number;

  @Column({ type: 'smallint', default: RefundStatus.PENDING })
  status: RefundStatus;

  @Column({ name: 'admin_note', length: 500, nullable: true })
  adminNote: string | null;

  @CreateDateColumn()
  createdAt: Date;

  @UpdateDateColumn()
  updatedAt: Date;
}
```

- [ ] **Step 4: entities/index.ts 追加 export**

```typescript
export { ShippingEntity } from './shipping.entity';
export { RefundEntity } from './refund.entity';
```

- [ ] **Step 5: 构建 server**

Run: `pnpm --filter server build`
Expected: 成功

- [ ] **Step 6: Commit**

```bash
git add packages/server/src/entities/
git commit -m "feat(server): OrderEntity 追加 coupon/discount/paidAt/shippedAt + 新增 Shipping/Refund Entity"
```

---

## Task 4: server Order DTO + Ship/Refund DTO

**Files:**

- Modify: `packages/server/src/modules/order/dto/create-order.dto.ts`
- Create: `packages/server/src/modules/order/dto/ship.dto.ts`
- Create: `packages/server/src/modules/order/dto/refund-request.dto.ts`
- Create: `packages/server/src/modules/order/dto/refund-review.dto.ts`

- [ ] **Step 1: CreateOrderDto 追加 addressId/couponId**

```typescript
import { IsString, IsOptional, MaxLength, IsInt, Min } from 'class-validator';
import { Type } from 'class-transformer';

export class CreateOrderDto {
  @IsString()
  @MaxLength(255)
  address: string;

  @IsString()
  @MaxLength(20)
  phone: string;

  @IsOptional()
  @IsString()
  @MaxLength(500)
  remark?: string;

  @IsOptional()
  @Type(() => Number)
  @IsInt()
  @Min(1)
  addressId?: number;

  @IsOptional()
  @Type(() => Number)
  @IsInt()
  @Min(1)
  couponId?: number;
}
```

- [ ] **Step 2: 创建 ship.dto.ts**

```typescript
import { IsString, MaxLength } from 'class-validator';

export class ShipDto {
  @IsString()
  @MaxLength(100)
  company: string;

  @IsString()
  @MaxLength(100)
  trackingNo: string;
}
```

- [ ] **Step 3: 创建 refund-request.dto.ts**

```typescript
import { IsString, MaxLength } from 'class-validator';

export class RefundRequestDto {
  @IsString()
  @MaxLength(500)
  reason: string;
}
```

- [ ] **Step 4: 创建 refund-review.dto.ts**

```typescript
import { IsOptional, IsString, MaxLength } from 'class-validator';

export class RefundReviewDto {
  @IsOptional()
  @IsString()
  @MaxLength(500)
  adminNote?: string;
}
```

- [ ] **Step 5: 构建 server + Commit**

Run: `pnpm --filter server build`

```bash
git add packages/server/src/modules/order/dto/
git commit -m "feat(server): Order DTO 追加 addressId/couponId + 新增 Ship/RefundRequest/RefundReview DTO"
```

---

## Task 5: server OrderService 状态流转方法（pay/ship/confirm/requestRefund）

**Files:**

- Modify: `packages/server/src/modules/order/order.service.ts`

- [ ] **Step 1: 在 OrderService 追加 4 方法**

在 `packages/server/src/modules/order/order.service.ts` 中，顶部 import 区追加：

```typescript
import { ShippingEntity, RefundEntity } from '../../entities';
import { ShipDto } from './dto/ship.dto';
import { RefundRequestDto } from './dto/refund-request.dto';
import { RefundReviewDto } from './dto/refund-review.dto';
import { RefundStatus } from 'shared';
```

在 `@InjectRepository(OrderItemEntity)` 之后追加：

```typescript
    @InjectRepository(ShippingEntity)
    private readonly shippingRepo: Repository<ShippingEntity>,
    @InjectRepository(RefundEntity)
    private readonly refundRepo: Repository<RefundEntity>,
```

并在顶部 Repository import 后追加这两个 repo 的注入（TypeOrmModule.forFeature 后续 Task 会注册）。

在 `cancel` 方法之后追加 4 个新方法：

```typescript
  async pay(userId: number, id: number) {
    const queryRunner = this.dataSource.createQueryRunner();
    await queryRunner.connect();
    await queryRunner.startTransaction();
    try {
      const rows: { id: number; status: number }[] = await queryRunner.manager.query(
        'SELECT id, status FROM orders WHERE id = ? AND user_id = ? FOR UPDATE',
        [id, userId],
      );
      if (rows.length === 0) {
        throw new NotFoundException(ErrorMessage[ErrorCode.ORDER_NOT_FOUND]);
      }
      if (rows[0].status !== OrderStatus.PENDING) {
        throw new BadRequestException(ErrorMessage[ErrorCode.ORDER_STATUS_ERROR]);
      }
      await queryRunner.manager.query(
        'UPDATE orders SET status = ?, paid_at = NOW() WHERE id = ?',
        [OrderStatus.PAID, id],
      );
      await queryRunner.commitTransaction();
    } catch (err) {
      await queryRunner.rollbackTransaction();
      throw err;
    } finally {
      await queryRunner.release();
    }
    return this.findOne(userId, id);
  }

  async ship(id: number, dto: ShipDto) {
    const queryRunner = this.dataSource.createQueryRunner();
    await queryRunner.connect();
    await queryRunner.startTransaction();
    try {
      const rows: { id: number; status: number }[] = await queryRunner.manager.query(
        'SELECT id, status FROM orders WHERE id = ? FOR UPDATE',
        [id],
      );
      if (rows.length === 0) {
        throw new NotFoundException(ErrorMessage[ErrorCode.ORDER_NOT_FOUND]);
      }
      if (rows[0].status !== OrderStatus.PAID) {
        throw new BadRequestException(ErrorMessage[ErrorCode.ORDER_STATUS_ERROR]);
      }
      const shipping = queryRunner.manager.create(ShippingEntity, {
        orderId: id,
        company: dto.company,
        trackingNo: dto.trackingNo,
        shippedAt: new Date(),
        status: 0,
      });
      await queryRunner.manager.save(ShippingEntity, shipping);
      await queryRunner.manager.query(
        'UPDATE orders SET status = ?, shipped_at = NOW() WHERE id = ?',
        [OrderStatus.SHIPPED, id],
      );
      await queryRunner.commitTransaction();
    } catch (err) {
      await queryRunner.rollbackTransaction();
      throw err;
    } finally {
      await queryRunner.release();
    }
    return this.findOneInternal(id);
  }

  async confirm(userId: number, id: number) {
    const queryRunner = this.dataSource.createQueryRunner();
    await queryRunner.connect();
    await queryRunner.startTransaction();
    try {
      const rows: { id: number; status: number }[] = await queryRunner.manager.query(
        'SELECT id, status FROM orders WHERE id = ? AND user_id = ? FOR UPDATE',
        [id, userId],
      );
      if (rows.length === 0) {
        throw new NotFoundException(ErrorMessage[ErrorCode.ORDER_NOT_FOUND]);
      }
      if (rows[0].status !== OrderStatus.SHIPPED) {
        throw new BadRequestException(ErrorMessage[ErrorCode.ORDER_STATUS_ERROR]);
      }
      await queryRunner.manager.query(
        'UPDATE orders SET status = ? WHERE id = ?',
        [OrderStatus.COMPLETED, id],
      );
      await queryRunner.commitTransaction();
    } catch (err) {
      await queryRunner.rollbackTransaction();
      throw err;
    } finally {
      await queryRunner.release();
    }
    return this.findOne(userId, id);
  }

  async requestRefund(userId: number, id: number, dto: RefundRequestDto) {
    const queryRunner = this.dataSource.createQueryRunner();
    await queryRunner.connect();
    await queryRunner.startTransaction();
    try {
      const rows: { id: number; status: number }[] = await queryRunner.manager.query(
        'SELECT id, status FROM orders WHERE id = ? AND user_id = ? FOR UPDATE',
        [id, userId],
      );
      if (rows.length === 0) {
        throw new NotFoundException(ErrorMessage[ErrorCode.ORDER_NOT_FOUND]);
      }
      const currentStatus = rows[0].status;
      if (currentStatus !== OrderStatus.PAID && currentStatus !== OrderStatus.SHIPPED) {
        throw new BadRequestException(ErrorMessage[ErrorCode.REFUND_NOT_ALLOWED]);
      }
      const refund = queryRunner.manager.create(RefundEntity, {
        orderId: id,
        userId,
        reason: dto.reason,
        prevStatus: currentStatus,
        status: RefundStatus.PENDING,
      });
      await queryRunner.manager.save(RefundEntity, refund);
      await queryRunner.manager.query(
        'UPDATE orders SET status = ? WHERE id = ?',
        [OrderStatus.REFUNDING, id],
      );
      await queryRunner.commitTransaction();
    } catch (err) {
      await queryRunner.rollbackTransaction();
      throw err;
    } finally {
      await queryRunner.release();
    }
    return this.findOne(userId, id);
  }

  // 内部使用：不校验 userId（Admin 路径）
  private async findOneInternal(id: number) {
    const order = await this.orderRepo.findOne({ where: { id } });
    if (!order) {
      throw new NotFoundException(ErrorMessage[ErrorCode.ORDER_NOT_FOUND]);
    }
    const items = await this.orderItemRepo.find({ where: { orderId: id } });
    return { ...order, items };
  }
```

- [ ] **Step 2: 构建 server**

Run: `pnpm --filter server build`
Expected: 成功（注意：OrderModule 还未 forFeature 注册 ShippingEntity/RefundEntity，本步骤 build 可能报 DI 错误，依赖 Task 6 才能完整运行；如 build 失败可放到 Task 6 后 commit）

- [ ] **Step 3: Commit（可与 Task 6 合并 commit）**

---

## Task 6: server OrderModule 注册新 Entity + Controller 路由

**Files:**

- Modify: `packages/server/src/modules/order/order.module.ts`
- Modify: `packages/server/src/modules/order/order.controller.ts`

- [ ] **Step 1: OrderModule forFeature 注册 Shipping/Refund**

```typescript
import { Module } from '@nestjs/common';
import { TypeOrmModule } from '@nestjs/typeorm';
import {
  OrderEntity,
  OrderItemEntity,
  CartEntity,
  ShippingEntity,
  RefundEntity,
} from '../../entities';
import { OrderService } from './order.service';
import { OrderController } from './order.controller';

@Module({
  imports: [
    TypeOrmModule.forFeature([
      OrderEntity,
      OrderItemEntity,
      CartEntity,
      ShippingEntity,
      RefundEntity,
    ]),
  ],
  controllers: [OrderController],
  providers: [OrderService],
  exports: [OrderService],
})
export class OrderModule {}
```

- [ ] **Step 2: OrderController 追加路由**

在 `packages/server/src/modules/order/order.controller.ts` 中，顶部 import 追加：

```typescript
import { RolesGuard } from '../../common/guards/roles.guard';
import { Roles } from '../../common/decorators/roles.decorator';
import { UserRole } from 'shared';
import { ShipDto } from './dto/ship.dto';
import { RefundRequestDto } from './dto/refund-request.dto';
```

在 `cancel` 方法之后追加：

```typescript
  @Put(':id/pay')
  pay(
    @CurrentUser('id') userId: number,
    @Param('id', ParseIntPipe) id: number,
  ) {
    return this.orderService.pay(userId, id);
  }

  @Put(':id/confirm')
  confirm(
    @CurrentUser('id') userId: number,
    @Param('id', ParseIntPipe) id: number,
  ) {
    return this.orderService.confirm(userId, id);
  }

  @Post(':id/refund')
  requestRefund(
    @CurrentUser('id') userId: number,
    @Param('id', ParseIntPipe) id: number,
    @Body() dto: RefundRequestDto,
  ) {
    return this.orderService.requestRefund(userId, id, dto);
  }

  @Get(':id/shipping')
  findShipping(
    @CurrentUser('id') userId: number,
    @Param('id', ParseIntPipe) id: number,
  ) {
    return this.orderService.findShipping(userId, id);
  }

  // Admin 发货（路径独立，避免与用户路由冲突）
  @Post('admin/:id/ship')
  @UseGuards(RolesGuard)
  @Roles(UserRole.ADMIN)
  ship(
    @Param('id', ParseIntPipe) id: number,
    @Body() dto: ShipDto,
  ) {
    return this.orderService.ship(id, dto);
  }
```

⚠️ 注意：`@Post('admin/:id/ship')` 必须声明在 `@Get(':id')` 之前或路径足够具体以避免路由冲突；实际建议把它放在 controller 类的最前面（在 @Post() 创建之前）。如担心冲突，也可拆为独立 AdminOrderController。本 plan 选用内联方式，放在所有 `:id` 路由之前。

- [ ] **Step 3: OrderService 追加 findShipping 方法**

在 `order.service.ts` 的 `requestRefund` 方法之后追加：

```typescript
  async findShipping(userId: number, orderId: number) {
    const order = await this.orderRepo.findOne({ where: { id: orderId, userId } });
    if (!order) {
      throw new NotFoundException(ErrorMessage[ErrorCode.ORDER_NOT_FOUND]);
    }
    return this.shippingRepo.findOne({ where: { orderId } });
  }
```

- [ ] **Step 4: 构建 server + 运行现有 e2e 确认无回归**

Run: `pnpm --filter server build`
Run: `pnpm --filter server test:e2e`
Expected: 全部通过

- [ ] **Step 5: Commit（Task 5 + Task 6 合并）**

```bash
git add packages/server/src/modules/order/
git commit -m "feat(server): OrderService 状态流转 pay/ship/confirm/requestRefund + Controller 路由"
```

---

## Task 7: server RefundService + RefundController（Admin 审批）

**Files:**

- Create: `packages/server/src/modules/refund/refund.module.ts`
- Create: `packages/server/src/modules/refund/refund.controller.ts`
- Create: `packages/server/src/modules/refund/refund.service.ts`
- Modify: `packages/server/src/app.module.ts`

- [ ] **Step 1: 创建 refund.service.ts**

`packages/server/src/modules/refund/refund.service.ts`：

```typescript
import { Injectable, NotFoundException, BadRequestException, Inject } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository, DataSource } from 'typeorm';
import {
  RefundEntity,
  OrderEntity,
  OrderItemEntity,
  ProductEntity,
  CartEntity,
} from '../../entities';
import { RefundReviewDto } from '../order/dto/refund-review.dto';
import { ErrorCode, ErrorMessage, OrderStatus, RefundStatus } from 'shared';
import { PinoLogger } from 'nestjs-pino';

@Injectable()
export class RefundService {
  constructor(
    @InjectRepository(RefundEntity)
    private readonly refundRepo: Repository<RefundEntity>,
    private readonly dataSource: DataSource,
    private readonly logger: PinoLogger,
  ) {
    this.logger.setContext(RefundService.name);
  }

  async findAll(query: { page?: number; limit?: number; status?: number }) {
    const page = query.page ?? 1;
    const limit = query.limit ?? 10;
    const qb = this.refundRepo.createQueryBuilder('r');
    if (query.status !== undefined) {
      qb.andWhere('r.status = :status', { status: query.status });
    }
    qb.orderBy('r.created_at', 'DESC')
      .skip((page - 1) * limit)
      .take(limit);
    const [list, total] = await qb.getManyAndCount();
    return { list, total, page, limit };
  }

  async approve(refundId: number) {
    const queryRunner = this.dataSource.createQueryRunner();
    await queryRunner.connect();
    await queryRunner.startTransaction();
    try {
      // 1. 锁 refund 行
      const refundRows: { id: number; status: number; order_id: number; prev_status: number }[] =
        await queryRunner.manager.query(
          'SELECT id, status, order_id, prev_status FROM refunds WHERE id = ? FOR UPDATE',
          [refundId],
        );
      if (refundRows.length === 0) {
        throw new NotFoundException(ErrorMessage[ErrorCode.REFUND_NOT_FOUND]);
      }
      const refundRow = refundRows[0];
      if (refundRow.status !== RefundStatus.PENDING) {
        throw new BadRequestException(ErrorMessage[ErrorCode.ORDER_STATUS_ERROR]);
      }
      const orderId = refundRow.order_id;

      // 2. 锁 order 行
      await queryRunner.manager.query('SELECT id FROM orders WHERE id = ? FOR UPDATE', [orderId]);

      // 3. 锁相关商品 + 回补库存
      const items = await queryRunner.manager.find(OrderItemEntity, { where: { orderId } });
      const productIds = items.map((i) => i.productId);
      if (productIds.length > 0) {
        await queryRunner.manager.query('SELECT id FROM products WHERE id IN (?) FOR UPDATE', [
          productIds,
        ]);
        for (const item of items) {
          await queryRunner.manager.query('UPDATE products SET stock = stock + ? WHERE id = ?', [
            item.quantity,
            item.productId,
          ]);
        }
      }

      // 4. 解绑优惠券（若有）
      const orderRows: { coupon_id: number | null }[] = await queryRunner.manager.query(
        'SELECT coupon_id FROM orders WHERE id = ?',
        [orderId],
      );
      const couponId = orderRows[0]?.coupon_id;
      if (couponId) {
        await queryRunner.manager.query(
          'UPDATE user_coupons SET order_id = NULL, used_at = NULL WHERE coupon_id = ? AND order_id = ?',
          [couponId, orderId],
        );
      }

      // 5. 更新状态
      await queryRunner.manager.query('UPDATE orders SET status = ? WHERE id = ?', [
        OrderStatus.REFUNDED,
        orderId,
      ]);
      await queryRunner.manager.query('UPDATE refunds SET status = ? WHERE id = ?', [
        RefundStatus.APPROVED,
        refundId,
      ]);

      await queryRunner.commitTransaction();
      this.logger.info({ refundId, orderId }, '退款通过，库存回补 + 券解绑');
    } catch (err) {
      await queryRunner.rollbackTransaction();
      throw err;
    } finally {
      await queryRunner.release();
    }
    return this.refundRepo.findOne({ where: { id: refundId } });
  }

  async reject(refundId: number, dto: RefundReviewDto) {
    const queryRunner = this.dataSource.createQueryRunner();
    await queryRunner.connect();
    await queryRunner.startTransaction();
    try {
      const refundRows: { id: number; status: number; order_id: number; prev_status: number }[] =
        await queryRunner.manager.query(
          'SELECT id, status, order_id, prev_status FROM refunds WHERE id = ? FOR UPDATE',
          [refundId],
        );
      if (refundRows.length === 0) {
        throw new NotFoundException(ErrorMessage[ErrorCode.REFUND_NOT_FOUND]);
      }
      const refundRow = refundRows[0];
      if (refundRow.status !== RefundStatus.PENDING) {
        throw new BadRequestException(ErrorMessage[ErrorCode.ORDER_STATUS_ERROR]);
      }

      // 恢复 prevStatus
      await queryRunner.manager.query('UPDATE orders SET status = ? WHERE id = ?', [
        refundRow.prev_status,
        refundRow.order_id,
      ]);
      await queryRunner.manager.query(
        'UPDATE refunds SET status = ?, admin_note = ? WHERE id = ?',
        [RefundStatus.REJECTED, dto.adminNote ?? null, refundId],
      );

      await queryRunner.commitTransaction();
      this.logger.info(
        { refundId, restoredStatus: refundRow.prev_status },
        '退款拒绝，恢复订单状态',
      );
    } catch (err) {
      await queryRunner.rollbackTransaction();
      throw err;
    } finally {
      await queryRunner.release();
    }
    return this.refundRepo.findOne({ where: { id: refundId } });
  }
}
```

- [ ] **Step 2: 创建 refund.controller.ts**

```typescript
import { Controller, Get, Post, Param, Body, Query, UseGuards, ParseIntPipe } from '@nestjs/common';
import { JwtAuthGuard } from '../../common/guards/jwt-auth.guard';
import { RolesGuard } from '../../common/guards/roles.guard';
import { Roles } from '../../common/decorators/roles.decorator';
import { UserRole } from 'shared';
import { RefundService } from './refund.service';
import { RefundReviewDto } from '../order/dto/refund-review.dto';

@Controller('admin/refunds')
@UseGuards(JwtAuthGuard, RolesGuard)
@Roles(UserRole.ADMIN)
export class RefundController {
  constructor(private readonly refundService: RefundService) {}

  @Get()
  findAll(
    @Query('page') page?: number,
    @Query('limit') limit?: number,
    @Query('status') status?: number,
  ) {
    return this.refundService.findAll({
      page: page ? Number(page) : undefined,
      limit: limit ? Number(limit) : undefined,
      status: status !== undefined ? Number(status) : undefined,
    });
  }

  @Post(':id/approve')
  approve(@Param('id', ParseIntPipe) id: number) {
    return this.refundService.approve(id);
  }

  @Post(':id/reject')
  reject(@Param('id', ParseIntPipe) id: number, @Body() dto: RefundReviewDto) {
    return this.refundService.reject(id, dto);
  }
}
```

- [ ] **Step 3: 创建 refund.module.ts**

```typescript
import { Module } from '@nestjs/common';
import { TypeOrmModule } from '@nestjs/typeorm';
import { RefundEntity, OrderEntity, OrderItemEntity, ProductEntity } from '../../entities';
import { RefundService } from './refund.service';
import { RefundController } from './refund.controller';

@Module({
  imports: [TypeOrmModule.forFeature([RefundEntity, OrderEntity, OrderItemEntity, ProductEntity])],
  controllers: [RefundController],
  providers: [RefundService],
})
export class RefundModule {}
```

- [ ] **Step 4: app.module.ts 注册 RefundModule**

在 `app.module.ts` 的 imports 数组中，在 `OrderModule,` 之后追加 `RefundModule,`，并在顶部 import。

- [ ] **Step 5: 构建 server**

Run: `pnpm --filter server build`
Expected: 成功

- [ ] **Step 6: Commit**

```bash
git add packages/server/src/modules/refund/ packages/server/src/app.module.ts
git commit -m "feat(server): 新增 Refund 模块（Admin 审批通过回补+解绑券，拒绝恢复 prevStatus）"
```

---

## Task 8: server order.flow e2e 测试

**Files:**

- Create: `packages/server/test/order.flow.e2e-spec.ts`

- [ ] **Step 1: 创建 e2e**

`packages/server/test/order.flow.e2e-spec.ts`（参考 `test/order.e2e-spec.ts` 模式）：

```typescript
import request from 'supertest';
import { TestHelper } from './helpers/test-helper';

describe('Order Flow (e2e)', () => {
  const helper = new TestHelper();
  let userToken: string;
  let adminToken: string;
  let orderId: number;

  beforeAll(async () => {
    await helper.setup();
    await helper.cleanDatabase();
    const admin = await helper.registerAdmin('13900000030', 'admin123456');
    adminToken = admin.accessToken;
    const user = await helper.registerAndLogin('13800000030', 'test123456');
    userToken = user.accessToken;

    // 清理 refunds/shippings
    const ds = helper.app.get((await import('typeorm')).DataSource);
    await ds.query('SET FOREIGN_KEY_CHECKS = 0');
    await ds.query('TRUNCATE TABLE refunds');
    await ds.query('TRUNCATE TABLE shippings');
    await ds.query('SET FOREIGN_KEY_CHECKS = 1');
  });

  afterAll(async () => {
    await helper.teardown();
  });

  it('完整流转：下单 → 支付 → 发货 → 确认收货', async () => {
    const productId = await helper.createProductAsAdmin(adminToken, {
      name: '流转测试商品',
      stock: 10,
    });
    await helper.addToCartAsUser(userToken, productId, '默认', 1);
    const createRes = await request(helper.httpServer)
      .post('/api/orders')
      .set('Authorization', `Bearer ${userToken}`)
      .send({ address: '北京市', phone: '13800000030' });
    expect(createRes.body.code).toBe(0);
    orderId = createRes.body.data.id;
    expect(createRes.body.data.status).toBe(0); // PENDING

    // 支付
    const payRes = await request(helper.httpServer)
      .put(`/api/orders/${orderId}/pay`)
      .set('Authorization', `Bearer ${userToken}`);
    expect(payRes.body.code).toBe(0);
    expect(payRes.body.data.status).toBe(1);
    expect(payRes.body.data.paidAt).toBeTruthy();

    // Admin 发货
    const shipRes = await request(helper.httpServer)
      .post(`/api/orders/admin/${orderId}/ship`)
      .set('Authorization', `Bearer ${adminToken}`)
      .send({ company: '顺丰', trackingNo: 'SF123456' });
    expect(shipRes.body.code).toBe(0);
    expect(shipRes.body.data.status).toBe(2);
    expect(shipRes.body.data.shippedAt).toBeTruthy();

    // 查物流
    const shippingRes = await request(helper.httpServer)
      .get(`/api/orders/${orderId}/shipping`)
      .set('Authorization', `Bearer ${userToken}`);
    expect(shippingRes.body.code).toBe(0);
    expect(shippingRes.body.data.company).toBe('顺丰');

    // 确认收货
    const confirmRes = await request(helper.httpServer)
      .put(`/api/orders/${orderId}/confirm`)
      .set('Authorization', `Bearer ${userToken}`);
    expect(confirmRes.body.code).toBe(0);
    expect(confirmRes.body.data.status).toBe(3); // COMPLETED
  });

  it('非法流转：PAID 不可再次支付', async () => {
    const res = await request(helper.httpServer)
      .put(`/api/orders/${orderId}/pay`)
      .set('Authorization', `Bearer ${userToken}`);
    expect(res.body.code).toBe(40402); // ORDER_STATUS_ERROR
  });

  it('非法流转：PENDING 不可发货', async () => {
    const productId = await helper.createProductAsAdmin(adminToken, { stock: 5 });
    await helper.addToCartAsUser(userToken, productId, '默认', 1);
    const createRes = await request(helper.httpServer)
      .post('/api/orders')
      .set('Authorization', `Bearer ${userToken}`)
      .send({ address: '北京市', phone: '13800000030' });
    const newOrderId = createRes.body.data.id;
    const res = await request(helper.httpServer)
      .post(`/api/orders/admin/${newOrderId}/ship`)
      .set('Authorization', `Bearer ${adminToken}`)
      .send({ company: '顺丰', trackingNo: 'SF999' });
    expect(res.body.code).toBe(40402);
  });

  it('退款流程：申请 → 通过（库存回补）', async () => {
    const productId = await helper.createProductAsAdmin(adminToken, { name: '退款商品', stock: 5 });
    await helper.addToCartAsUser(userToken, productId, '默认', 2);
    const stockBefore = await helper.getProductStock(productId);

    const createRes = await request(helper.httpServer)
      .post('/api/orders')
      .set('Authorization', `Bearer ${userToken}`)
      .send({ address: '北京市', phone: '13800000030' });
    const refundOrderId = createRes.body.data.id;
    const stockAfterOrder = await helper.getProductStock(productId);
    expect(stockAfterOrder).toBe(stockBefore - 2);

    await request(helper.httpServer)
      .put(`/api/orders/${refundOrderId}/pay`)
      .set('Authorization', `Bearer ${userToken}`);
    const refundReqRes = await request(helper.httpServer)
      .post(`/api/orders/${refundOrderId}/refund`)
      .set('Authorization', `Bearer ${userToken}`)
      .send({ reason: '商品损坏' });
    expect(refundReqRes.body.code).toBe(0);
    expect(refundReqRes.body.data.status).toBe(5); // REFUNDING

    // Admin 查退款列表
    const listRes = await request(helper.httpServer)
      .get('/api/admin/refunds')
      .set('Authorization', `Bearer ${adminToken}`);
    expect(listRes.body.code).toBe(0);
    const refundId = listRes.body.data.list[0].id;

    // 通过退款
    const approveRes = await request(helper.httpServer)
      .post(`/api/admin/refunds/${refundId}/approve`)
      .set('Authorization', `Bearer ${adminToken}`);
    expect(approveRes.body.code).toBe(0);

    const stockAfterRefund = await helper.getProductStock(productId);
    expect(stockAfterRefund).toBe(stockBefore); // 回补
  });

  it('退款拒绝：恢复 prevStatus', async () => {
    const productId = await helper.createProductAsAdmin(adminToken, { stock: 10 });
    await helper.addToCartAsUser(userToken, productId, '默认', 1);
    const createRes = await request(helper.httpServer)
      .post('/api/orders')
      .set('Authorization', `Bearer ${userToken}`)
      .send({ address: '北京市', phone: '13800000030' });
    const rejectOrderId = createRes.body.data.id;
    await request(helper.httpServer)
      .put(`/api/orders/${rejectOrderId}/pay`)
      .set('Authorization', `Bearer ${userToken}`);
    await request(helper.httpServer)
      .post(`/api/orders/${rejectOrderId}/refund`)
      .set('Authorization', `Bearer ${userToken}`)
      .send({ reason: '不想要了' });

    const listRes = await request(helper.httpServer)
      .get('/api/admin/refunds')
      .set('Authorization', `Bearer ${adminToken}`);
    const refundId = listRes.body.data.list[0].id;

    const rejectRes = await request(helper.httpServer)
      .post(`/api/admin/refunds/${refundId}/reject`)
      .set('Authorization', `Bearer ${adminToken}`)
      .send({ adminNote: '不符合退款条件' });
    expect(rejectRes.body.code).toBe(0);

    // 订单恢复为 PAID
    const orderRes = await request(helper.httpServer)
      .get(`/api/orders/${rejectOrderId}`)
      .set('Authorization', `Bearer ${userToken}`);
    expect(orderRes.body.data.status).toBe(1); // PAID
  });
});
```

- [ ] **Step 2: 运行 e2e**

⚠️ docker compose ps healthy

Run: `pnpm --filter server test:e2e -- test/order.flow.e2e-spec.ts`
Expected: 5 用例通过

- [ ] **Step 3: Commit**

```bash
git add packages/server/test/order.flow.e2e-spec.ts
git commit -m "test(order): 完整状态流转 e2e（pay/ship/confirm + 退款通过/拒绝 + 非法流转）"
```

---

## Task 9: web OrderDetail.tsx 按 status 显示按钮

**Files:**

- Modify: `packages/web/src/pages/OrderDetail.tsx`

- [ ] **Step 1: 读 OrderDetail.tsx 定位 status 与按钮区**

读 `packages/web/src/pages/OrderDetail.tsx`，找到现有 STATUS_LABELS / STATUS_BG 定义与底部按钮区（约 170-188 行）。

- [ ] **Step 2: 追加 REFUNDING/REFUNDED 到 STATUS_LABELS**

```tsx
const STATUS_LABELS: Record<number, string> = {
  0: '待付款',
  1: '已付款',
  2: '已发货',
  3: '已完成',
  4: '已取消',
  5: '退款审核中',
  6: '已退款',
};
```

- [ ] **Step 3: 追加状态按钮**

在底部按钮区按 status 显示：

- PENDING(0)：「去支付」按钮 → 调 `PUT /orders/:id/pay`
- SHIPPED(2)：「确认收货」按钮 → 调 `PUT /orders/:id/confirm`
- PAID(1) 或 SHIPPED(2)：「申请退款」按钮 → 弹 modal 填 reason → 调 `POST /orders/:id/refund`
- COMPLETED(3)：「去评价」按钮（P2-B Task 14 会接入实际评价，本 task 占位即可）
- REFUNDING(5)/REFUNDED(6)：纯文案显示

需新增 `orderApi.pay/confirm/requestRefund` 方法（在 api/order.ts）。

- [ ] **Step 4: api/order.ts 追加方法**

```typescript
pay(id: number) { return apiClient.put<ApiResponse<Order>>(`/orders/${id}/pay`); },
confirm(id: number) { return apiClient.put<ApiResponse<Order>>(`/orders/${id}/confirm`); },
requestRefund(id: number, reason: string) {
  return apiClient.post<ApiResponse<Order>>(`/orders/${id}/refund`, { reason });
},
getShipping(id: number) {
  return apiClient.get<ApiResponse<Shipping | null>>(`/orders/${id}/shipping`);
},
```

- [ ] **Step 5: 运行 web build**

Run: `pnpm --filter web build`
Expected: 成功

- [ ] **Step 6: Commit**

```bash
git add packages/web/src/pages/OrderDetail.tsx packages/web/src/api/order.ts
git commit -m "feat(web): OrderDetail 按 status 显示 pay/confirm/refund 按钮 + 追加 REFUNDING/REFUNDED 标签"
```

---

## Task 10: web AdminRefunds 页 + 路由

**Files:**

- Create: `packages/web/src/pages/AdminRefunds.tsx`
- Create: `packages/web/src/api/refund.ts`
- Modify: `packages/web/src/router/index.tsx`

- [ ] **Step 1: 创建 api/refund.ts**

```typescript
import { apiClient } from './client';
import type { ApiResponse, Refund, PaginatedResponse } from 'shared';

export const refundApi = {
  list(params?: { page?: number; limit?: number; status?: number }) {
    return apiClient.get<ApiResponse<PaginatedResponse<Refund>>>('/admin/refunds', { params });
  },
  approve(id: number) {
    return apiClient.post<ApiResponse<Refund>>(`/admin/refunds/${id}/approve`);
  },
  reject(id: number, adminNote: string) {
    return apiClient.post<ApiResponse<Refund>>(`/admin/refunds/${id}/reject`, { adminNote });
  },
};
```

- [ ] **Step 2: 创建 AdminRefunds.tsx**

仿 AdminBanners 模式：

- 表格列：订单号 / 用户 / reason / 状态（待审批/已通过/已拒绝）/ 操作
- 操作：「通过」直接调 approve；「拒绝」弹 modal 填 adminNote → 调 reject
- 沿用 gray-200 border + rounded-2xl 样式

（具体代码参考 AdminBanners.tsx 结构，~150 行）

- [ ] **Step 3: router 新增 /admin/refunds 路由**

```tsx
const AdminRefunds = lazy(() => import('@/pages/AdminRefunds'));
// 在 /admin/banners 之后：
{ path: '/admin/refunds', element: <SuspenseWrapper><AdminRoute><AdminRefunds /></AdminRoute></SuspenseWrapper> },
```

- [ ] **Step 4: 运行 web build + Commit**

```bash
git add packages/web/src/pages/AdminRefunds.tsx packages/web/src/api/refund.ts packages/web/src/router/index.tsx
git commit -m "feat(web): 新增 AdminRefunds 退款审批页 + /admin/refunds 路由"
```

---

## Checkpoint 1: P2-A 全量回归

- [ ] **Step 1: shared 重 build**

Run: `pnpm --filter shared build`

- [ ] **Step 2: server 全量 unit**

Run: `pnpm --filter server test`
Expected: 全部通过

- [ ] **Step 3: server 全量 e2e**

Run: `pnpm --filter server test:e2e`
Expected: 全部通过（含新增 order.flow）

- [ ] **Step 4: web build**

Run: `pnpm --filter web build`

- [ ] **Step 5: 如无回归，继续 Section 2（P2-B）**

---

# Section 2: P2-B 用户互动（地址簿 + 评价 + 收藏）

> 由于 plan 已超长，P2-B/C/D/E 各 Section 的 Task 细节参考 Section 1 的格式编写。每个 Section 约 6-10 个 Task，包含：shared 类型已 Task 2 完成，server entity/module/e2e，web api/pages/components，组尾 Checkpoint。

## Task 11-15: P2-B server Address Module（entity/module/controller/service/dto/e2e）

参考 Section 1 Task 3/4/6/7/8 模式：

- `entities/address.entity.ts` 新建
- `modules/address/` 新建（CRUD + setDefault 事务）
- DTO 含 CreateAddressDto/UpdateAddressDto
- e2e `test/address.e2e-spec.ts`（CRUD + 默认不可删 + 设默认）
- OrderService.create 支持 addressId 快照写入

## Task 16-19: P2-B server Review Module

- `entities/review.entity.ts` 新建（Unique(orderId, productId)）
- `modules/review/` 新建（GET products/:id/reviews @Public、POST orders/:id/reviews JWT、GET reviews/mine JWT）
- 评价资格校验：订单 COMPLETED + 归属 + 未重复
- e2e `test/review.e2e-spec.ts`

## Task 20-23: P2-B server Favorite Module

- `entities/favorite.entity.ts` 新建（Unique(userId, productId)）
- `modules/favorite/` 新建（4 接口）
- e2e `test/favorite.e2e-spec.ts`

## Task 24-27: P2-B web 前端

- `api/{address,review,favorite}.ts`
- `pages/{Addresses,Favorites}.tsx` + 路由
- `components/{ReviewSection,FavoriteToggle}.tsx`
- `Checkout.tsx` 地址区域接地址簿
- `ProductDetail.tsx` 插入 ReviewSection + FavoriteToggle
- `OrderDetail.tsx` COMPLETED 显示「去评价」
- `Profile.tsx` 追加菜单项

## Checkpoint 2: P2-B 全量回归

---

# Section 3: P2-C 优惠券

## Task 28-32: P2-C server Coupon Module

- `entities/{coupon-template,user-coupon}.entity.ts` 新建
- `modules/coupon/` 新建（available/mine/claim/preview/Admin CRUD）
- claim 事务（totalCount 校验 + claimedCount++）
- order.service.create 支持 couponId（事务内核销）
- order.service.cancel/approveRefund 券解绑
- e2e `test/coupon.e2e-spec.ts`

## Task 33-35: P2-C web 前端

- `api/coupon.ts`
- `pages/{AdminCoupons,MyCoupons}.tsx` + 路由
- `Checkout.tsx` 优惠券选择 modal + preview
- `Profile.tsx` 菜单项

## Checkpoint 3: P2-C 全量回归

---

# Section 4: P2-D 搜索增强

## Task 36-38: P2-D server

- QueryProductDto 扩展 + findAll 多维筛选 + cacheKey
- GET /products/bestsellers + GET /products/suggest
- e2e `test/product.search.e2e-spec.ts`

## Task 39-40: P2-D web

- `SearchBar.tsx` 加联想浮层
- Home.tsx 可选筛选栏

## Checkpoint 4: P2-D 全量回归

---

# Section 5: P2-E 基础设施

## Task 41-44: P2-E server Upload + Category CRUD

- 安装 multer + @types/multer
- `modules/upload/` 新建（FileInterceptor + 2MB + MIME）
- main.ts useStaticAssets
- Dockerfile mkdir uploads + VOLUME
- CategoryController 追加 POST/PUT/DELETE + ProductService 追加方法
- e2e `test/{upload,category.crud}.e2e-spec.ts`

## Task 45-47: P2-E web

- `components/UploadButton.tsx`
- AdminProducts/AdminBanners image 字段改上传组件
- `pages/AdminCategories.tsx` + 路由
- Profile 头像编辑扩展上传

## Checkpoint 5: P2-E 全量回归 + 最终验收

- [ ] **Step 1: 全量构建与测试**
  - `pnpm --filter shared build`
  - `pnpm --filter server test`
  - `pnpm --filter server test:e2e`
  - `pnpm --filter web build`

- [ ] **Step 2: 手动场景验证（docker compose）**
      走查 spec 第 11 节全部 35 项场景（P2-A 7 + P2-B 10 + P2-C 8 + P2-D 4 + P2-E 6）

- [ ] **Step 3: GDD assertions 对应检查**
      L4-1~~L4-5 e2e 全部覆盖；L3-1~~L3-5 契约/集成测试覆盖；L2-1~L2-6 单测覆盖；L1-1/L1-2 编译/枚举穷举

---

## 验收映射（Contract → Task）

| Contract DoD                       | Task       |
| ---------------------------------- | ---------- |
| shared Order 扩展                  | Task 1     |
| shared 7 类型 + 业务码             | Task 2     |
| OrderEntity 扩展 + Shipping/Refund | Task 3     |
| Order DTO + Ship/Refund DTO        | Task 4     |
| OrderService 状态流转 4 方法       | Task 5     |
| OrderModule + Controller 路由      | Task 6     |
| RefundService + Controller         | Task 7     |
| order.flow e2e                     | Task 8     |
| OrderDetail 前端按钮               | Task 9     |
| AdminRefunds 页                    | Task 10    |
| Address Module                     | Task 11-15 |
| Review Module                      | Task 16-19 |
| Favorite Module                    | Task 20-23 |
| P2-B 前端                          | Task 24-27 |
| Coupon Module                      | Task 28-32 |
| P2-C 前端                          | Task 33-35 |
| 搜索增强 server                    | Task 36-38 |
| 搜索增强 web                       | Task 39-40 |
| Upload + Category server           | Task 41-44 |
| P2-E 前端                          | Task 45-47 |

---

## Self-Review Notes

- **Spec coverage**: spec 第 5-9 节 11 子项全部映射到 Task 1-47；GDD 段 13 个 Level Items 对应到 e2e/单测/编译验证
- **Placeholder scan**: Section 2-5 的 Task 标题为「参考 Section 1 模式」——这是 plan 长度管理的折中。实际执行时 implementer subagent 会基于 spec 第 6-9 节的具体设计（含完整字段定义与接口表）生成代码。如需完整展开，plan 会超 3000 行。当前结构在 SDD 执行时由 subagent 基于 spec 填充细节
- **Type consistency**: OrderStatus/RefundStatus/CouponType 用 enum（shared 定义，server/web 共用）；所有 DTO 字段与 spec 第 5-9 节一致
- **Task 依赖**: Task 1-2（shared）必须最先；Task 3-10（P2-A）依赖 Task 1-2；Task 11-27（P2-B）独立；Task 28-35（P2-C）影响 OrderEntity 需在 P2-A 后；Task 36-40（P2-D）独立；Task 41-47（P2-E）独立
- **Checkpoint**: 每个 Section 结束插入全量回归，避免错误累积

## 注意

本 plan 是「结构化骨架 + 关键 Task 详写」模式：Section 1（P2-A）的 Task 1-10 含完整代码；Section 2-5 的 Task 标题描述范围与产出物，具体实现代码由 SDD subagent 基于 spec 第 6-9 节（含完整 entity 字段、接口表、前端组件契约）生成。如需每个 Task 都含完整代码，请明确告知，我会补全（plan 会超长）。
